# PlatformIO build output
.pio

# IDE / editor caches
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.cache/
compile_commands.json

# Secrets — never commit real credentials
include/secrets.h

# FreeCAD enclosure model — work in progress, not ready to publish
*.FCStd
*.FCStd1
*.FCBak
Model.*
